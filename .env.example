# MCP Skeleton Server Configuration
# Copy this file to .env and update with your values

# ============================================================================
# Server Configuration
# ============================================================================

# Server identification
SERVER_NAME=MCP Skeleton Server
SERVER_VERSION=0.1.0

# Logging level: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# ============================================================================
# HTTP/SSE Transport Configuration (for Docker/Kubernetes deployments)
# ============================================================================

# Host to bind to (0.0.0.0 for all interfaces in containers)
MCP_SERVER_HOST=0.0.0.0

# Port to listen on
MCP_SERVER_PORT=8000

# ============================================================================
# Custom Configuration (Add your own settings here)
# ============================================================================

# Example: Database connection string
# DATABASE_URL=postgresql://user:password@localhost:5432/dbname

# Example: API keys for external services
# OPENAI_API_KEY=your_api_key_here
# ANTHROPIC_API_KEY=your_api_key_here

# Example: Feature flags
# ENABLE_FEATURE_X=true
# ENABLE_FEATURE_Y=false

# Example: Custom timeouts
# REQUEST_TIMEOUT=30
# CONNECTION_TIMEOUT=10

# Example: External service URLs
# EXTERNAL_API_URL=https://api.example.com
# WEBHOOK_URL=https://webhook.example.com

# ============================================================================
# Notes
# ============================================================================
# - All settings have defaults in utilities/config.py
# - Environment variables take precedence over .env file
# - Never commit .env file to version control (only .env.example)
# - In Kubernetes, use ConfigMaps and Secrets instead of .env
